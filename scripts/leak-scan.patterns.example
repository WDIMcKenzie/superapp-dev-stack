# Optional org-specific leak-scan patterns (one extended regex per line).
# Copy to scripts/leak-scan.patterns (gitignored) and customize for YOUR org.
# Do not commit real hostnames, project IDs, or product codenames to this public repo.
#
# Examples (replace with your placeholders):
# {{ORG}}-prod|{{ORG}}-staging|internal\.{{ORG}}\.com
# {{COMPANY_REPO_SLUG}}|docs/plans/20[0-9]{2}-
