# Trigger: full interactive setup

**Say:** `run setup wizard` or `interactive init`

From curriculum repo root:

```bash
pnpm run init
```

Use when the user is new and wants prompts for tools, workspace, database, and AI tools.

Does not deploy to cloud.
