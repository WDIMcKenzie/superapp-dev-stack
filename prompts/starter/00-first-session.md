# Starter prompt — First session

Copy into your AI tool after pasting `team/ADAPTER-PROMPT.md`:

---

I just finished SuperApp init. My project folder is `{{PROJECT_PATH}}/` (see `AI-START-HERE.md`).

1. Read `team/STATE.md` and `team/HANDOFF.md`.
2. Run `pnpm run db:validate` from the project root.
3. Tell me what is working and what I should do next (one short checklist).

Do not ask for secrets. Use `.env.example` for variable names only.
