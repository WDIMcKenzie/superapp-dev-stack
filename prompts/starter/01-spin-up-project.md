# Starter prompt — Spin up from blueprint

---

Act as **Orchestrator**. I have a feature blueprint (paste below).

1. Split work into Engineer / Researcher / Quality tasks.
2. Update `team/STATE.md` with the plan (no secrets).
3. Give the **Engineer** a single first implementation slice for `{{PROJECT_PATH}}/` only.

Blueprint:

```
(paste here)
```
