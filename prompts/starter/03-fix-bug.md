# Starter prompt — Fix a bug

---

Act as **Engineer**. Debug systematically in `{{PROJECT_PATH}}/`:

1. Reproduce (command or URL).
2. Find root cause with evidence (logs, test output).
3. Minimal fix — no drive-by refactors.
4. Add or update a test if the bug could regress.

Symptom:

```
(paste error, test name, or behavior)
```
