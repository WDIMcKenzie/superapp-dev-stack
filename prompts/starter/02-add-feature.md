# Starter prompt — Add one feature

---

Act as **Engineer** in `{{PROJECT_PATH}}/`:

- Implement only the scope below.
- Match existing patterns under `apps/`, `services/`, `packages/`.
- Run tests relevant to your change before saying done.

Scope:

```
(describe feature)
```
