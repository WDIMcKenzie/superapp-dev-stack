# Starter prompt — WordPress / shared host track

---

Act as **Engineer** for the WordPress site in `{{PROJECT_PATH}}/sites/wordpress-site/`:

- PHP + WordPress coding standards, hooks only (no core edits).
- Theme changes in child theme pattern if applicable.
- Document any manual steps for GoDaddy / Bluehost / Hostinger in comments.

Task:

```
(describe site change)
```
