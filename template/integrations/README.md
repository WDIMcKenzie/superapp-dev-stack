# Integrations

External webhooks and third-party connectors live here.

## Webhook stub

See `example-webhook/` for idempotent handler pattern.

Do not store API keys in this tree — use environment variables.
