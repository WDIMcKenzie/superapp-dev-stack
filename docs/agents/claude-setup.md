# Claude setup

Use `CLAUDE.md` + `team/Orchestrator/AGENTS.md`.

Paste `team/ADAPTER-PROMPT.md` at session start.

Orchestrator produces blueprints; hand implementation to Cursor Engineer.

Blueprint template: `docs/agents/templates/blueprint-template.md`.
