# HANDOFF.md

> Single most-recent message only. Archive older content under the sender's role folder.

## To

Orchestrator → User

## From

Template (initial)

## Summary

Repository scaffold ready. Initialize with `team/ADAPTER-PROMPT.md` and read
`docs/00-start-here.md`.

## Next

1. Choose host + database from `docs/hosting/database-host-pairing.md`
2. Copy `template/` to a new project directory
3. Run `pnpm install`, `pnpm run db:up`, `pnpm run db:validate`, `pnpm dev`
