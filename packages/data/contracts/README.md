# MongoDB collection contracts

When `DATABASE_MIGRATE_MODE=manual`, document collections here.

## Example

```typescript
// users: { _id, email, createdAt }
```

Keep in sync with application code and Orchestrator blueprints.
